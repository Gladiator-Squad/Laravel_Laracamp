name: Docker Compose

on:
  push:
    branches:
      - feature/setup-continuous-deployment-process

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Compose
      run: |
        docker-compose --version
        sudo rm /usr/local/bin/docker-compose
        curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose
    
    - name: Build Image for Docker compose
      run: |
        docker-compose build app
      working-directory: ./app

  #   - name: Build and start Docker Compose
  #     run: |
  #       docker-compose up -d
  #     working-directory: ./app

  #   - name: Build and push Docker image
  #     run: |
  #       docker-compose build app
  #       docker login -u your-dockerhub-username -p your-dockerhub-password
  #       docker tag your-image-name:latest your-dockerhub-username/your-image-name:latest
  #       docker push your-dockerhub-username/your-image-name:latest
  #     working-directory: ./app

  # build:
  #   runs-on: ubuntu-latest
  #   steps:
    
  #   - name: SSH into IDCloudHost and update Docker containers
  #     uses: appleboy/ssh-action@master
  #     with:
  #       host: ${{ secrets.IDCLOUDHOST_HOST }}
  #       username: ${{ secrets.IDCLOUDHOST_USERNAME }}
  #       key: ${{ secrets.IDCLOUDHOST_SSH_KEY }}
  #       port: 22
  #       script: |
  #         docker pull your-dockerhub-username/your-image-name:latest
  #         docker run -d --name your-container-name -p 80:80 your-dockerhub-username/your-image-name:latest
