name: Main CI Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    extends:
      template: .github/workflows/templates/build-deploy-template.yml


  run-tests:
    uses: ./.github/workflows/ci-test.yml
