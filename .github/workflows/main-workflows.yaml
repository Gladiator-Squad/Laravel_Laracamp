name: Main CI Workflow

on:
  push:
    branches:
      - DEV-5_Setup-Pipeline-Github-Action-for-Laracamp-Project
  workflow_dispatch:

jobs:
  build-and-deploy:
    uses: ./.github/workflows/ci-build-deploy.yml

  run-tests:
    uses: ./.github/workflows/ci-test.yml
